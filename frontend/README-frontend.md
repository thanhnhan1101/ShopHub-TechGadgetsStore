Frontend instructions (Vue 3 + Vite)

Requirements: Node 18+ (or current LTS), npm

To install and run dev server:

powershell
cd frontend
npm install
npm run dev

The dev server runs at http://localhost:5173 by default.
