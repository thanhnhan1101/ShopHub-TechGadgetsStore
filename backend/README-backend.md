Backend instructions (Spring Boot)

Requirements: Java 17+, Maven

To build:

powershell
cd backend
mvn -DskipTests package

To run:

powershell
cd backend
mvn spring-boot:run

Configure `application.properties` for SQL Server credentials and URL.
